{
  "lexicon": 1,
  "id": "app.aozora.convo.deleteMessage",
  "description": "Unsend (tombstone) the caller's own previously-sent message. Structurally self-sovereign: delete-record can only ever touch a URI under the caller's own repo, so this can never delete someone else's message — passing an rkey the caller never sent is a harmless no-op, not an error. Once tombstoned, the message is dropped from every read projection (listMessages/getConvo) automatically. Reactions/receipts/attachments already attached to this message's rkey are NOT cascade-deleted (a deliberate, documented scope limit, not an oversight).",
  "defs": {
    "main": {
      "type": "procedure",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "convoId": { "type": "string" },
            "rkey": { "type": "string" }
          },
          "required": ["convoId", "rkey"]
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "convoId": { "type": "string" },
            "rkey": { "type": "string" },
            "did": { "type": "string" }
          },
          "required": ["convoId", "rkey", "did"]
        }
      }
    }
  }
}
